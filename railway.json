{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn install --frozen-lockfile && yarn mcp:build"
  },
  "deploy": {
    "startCommand": "yarn mcp:serve",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
